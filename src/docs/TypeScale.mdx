import { Meta, Subtitle, Title, Typeset } from '@storybook/addon-docs/blocks';
import typography from '../design-tokens/typography';
import TypeScale from './TypeScale';

<Meta title="Design Tokens/Type Scale" />

export const sampleText = 'We are made of starstuff.';

<Title>Type Scale</Title>

<Subtitle>Font Family: Eurostile</Subtitle>

**Font Weight:** Regular ({typography.weight.regular})

<TypeScale
  config={{
    baseSize: typography.baseSize,
    fontFamily: typography.family.primary,
    fontWeight: typography.weight.regular,
    sampleText: sampleText,
  }}
  scales={Object.entries(typography.size).map(([key, size]) => ({
    name: key,
    value: size,
  }))}
/>

**Font Weight:** Light ({typography.weight.light})

<TypeScale
  config={{
    baseSize: typography.baseSize,
    fontFamily: typography.family.primary,
    fontWeight: typography.weight.light,
    sampleText: sampleText,
  }}
  scales={Object.entries(typography.size).map(([key, size]) => ({
    name: key,
    value: size,
  }))}
/>

**Font Weight:** Semibold ({typography.weight.semibold})

<TypeScale
  config={{
    baseSize: typography.baseSize,
    fontFamily: typography.family.primary,
    fontWeight: typography.weight.semibold,
    sampleText: sampleText,
  }}
  scales={Object.entries(typography.size).map(([key, size]) => ({
    name: key,
    value: size,
  }))}
/>

**Font Weight:** Bold ({typography.weight.bold})

<TypeScale
  config={{
    baseSize: typography.baseSize,
    fontFamily: typography.family.primary,
    fontWeight: typography.weight.bold,
    sampleText: sampleText,
  }}
  scales={Object.entries(typography.size).map(([key, size]) => ({
    name: key,
    value: size,
  }))}
/>
